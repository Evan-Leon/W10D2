import React from 'react';
import Clock from './clock';

const Root = (props) => {
    return (
        <div className="root">
            <h1>Widget Application</h1>
            < Clock />
        </div>
    )
}

export default Root;